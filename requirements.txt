# BCM Application — runtime dependencies
# Python 3.12. Install into a venv:
#   python -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements.txt
#
# Django 5.2 LTS (supported to April 2028). Do NOT drop to 5.1 — it reached
# end-of-life in December 2025.

# --- Core framework ---
Django==5.2.17
asgiref==3.12.1
sqlparse==0.6.0
tzdata==2026.4

# --- REST API ---
djangorestframework==3.18.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==26.1
drf-spectacular==0.30.0

# --- Configuration ---
# django-environ is the ONE env loader (AD-11). python-dotenv is deliberately
# absent — do not reintroduce it.
django-environ==0.14.0

# --- Database ---
mysqlclient==2.2.8

# --- Authentication / SSO (Phase 1) ---
social-auth-app-django==6.0.1
social-auth-core==5.1.0
PyJWT==2.14.0
cryptography==50.0.1

# --- Async tasks & cache ---
celery==5.6.3
redis==8.1.0
django-redis==7.0.0

# --- Security ---
django-ratelimit==4.1.0

# --- Misc ---
pillow==12.3.0
requests==2.34.2
python-docx==1.2.0
reportlab==5.0.1
openpyxl==3.1.5
et-xmlfile==2.0.0

# --- Production runtime and observability (Phase 10) ---
gunicorn==26.2.0
sentry-sdk==2.69.1

# --- Browser push notifications ---
pywebpush==2.5.0
py-vapid==1.9.4
