from django.apps import AppConfig


class HelpcenterConfig(AppConfig):
    """BCM team document library."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.helpcenter"
    label = "helpcenter"
