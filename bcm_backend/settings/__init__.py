"""
Settings package.

Select an environment with DJANGO_SETTINGS_MODULE, e.g.
    bcm_backend.settings.development   (default in manage.py)
    bcm_backend.settings.production

There is deliberately no star-import of an environment here: importing this
package must not silently pick a configuration.
"""
